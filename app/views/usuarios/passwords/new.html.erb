<!-- app/views/usuarios/passwords/new.html.erb -->

<div class="container mt-5" style="max-width: 400px;">
  <h2 class="mb-4 text-center">Recuperar senha</h2>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f| %>
    <div class="mb-3">
      <%= f.label :email, "E-mail" %>
      <%= f.email_field :email, autofocus: true, class: "form-control" %>
    </div>
    <%= f.submit "Enviar instruções", class: "btn btn-primary w-100" %>
  <% end %>
  <div class="mt-3 text-center">
    <%= link_to "Voltar para o login", new_usuario_session_path %> |
    <%= link_to "Cadastrar", new_usuario_registration_path %>
  </div>
</div>
