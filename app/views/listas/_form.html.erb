<%= form_with(model: lista) do |form| %>
  <% if lista.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lista.errors.count, "error") %> prohibited this lista from being saved:</h2>

      <ul>
        <% lista.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_field :descricao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
